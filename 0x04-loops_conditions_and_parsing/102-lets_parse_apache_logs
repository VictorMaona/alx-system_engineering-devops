#!/usr/bin/env bash
# Displays the visitor IP with the HTTP status code from Apache log file.

# Use awk to extract and format the IP and HTTP status code from the Apache log file
awk '{print $1, $9}' /path/to/apache-access.log
